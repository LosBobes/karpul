# Copy to deploy/.env on the server (the deploy workflow writes it from GitHub secrets).
DOMAIN=karpul.example.com
# Optional seed for the company-car pool, applied on first start only.
# CORPORATE_CARS=Skoda Octavia|B-123-XY|4;VW Transporter|B-456-ZZ|8
CORPORATE_CARS=

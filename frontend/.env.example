# Optional. Leave empty to call the API on the same origin (dev proxy / nginx / FastAPI static hosting).
VITE_API_URL=
# Dev-server proxy target for /api (default http://localhost:8000)
VITE_API_PROXY=http://localhost:8000
